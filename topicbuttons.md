Key lists:

TOPIC_BUTTONS — the labels shown as topic bubbles (what the client displays).
TOPIC_PROMPTS — mapping label → prompt used when a topic is selected.
SUPPORTED_TOPICS — used for topic detection/heuristics (not directly the UI list).